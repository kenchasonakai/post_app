<div class="flex items-center justify-center">
  <div class="w-full max-w-2xl rounded-lg p-8">
    <% @post_templates.each do |post_template| %>
      <div class="card bg-base-100 w-full shadow-xl border-double border-4 border-sky-500 mb-3">
        <div class="card-body relative">
          <div class="flex align-center">
            <div class="avatar placeholder">
              <div class="bg-neutral text-neutral-content w-12 rounded-full">
                <span><%= post_template.user.nickname_initial %></span>
              </div>
            </div>
            <div class="flex flex-col ps-3">
              <p>by <%= "@#{post_template.user.nickname}" %></p>
              <p><%= post_template.created_at.strftime("%Y年%m月%d日") %></p>
            </div>
          </div>
          <div class="ms-12">
            <h2 class="card-title"><%= link_to post_template.title, post_template_path(post_template), class: "hover:underline after:absolute after:inset-0 mb-2" %></h2>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>